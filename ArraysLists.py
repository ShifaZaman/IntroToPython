#Introduction to arrays/lists
values=["Bananas","Apples","Mangoes","Pomelo"] #List contains multiple values and starts at position 0
print(values[2])

numbers = [1,3,5,7]

for x in range(0,4): #x is the first number in the bracket and counts up to the last number but doesn't include the last number. The number is saved on the value x. It increases by 1 each time and plugs it back into the list brackets to call that value from the lists
  print(numbers[x])

'''print(numbers[0])
print(numbers[1])
print(numbers[2])
print(numbers[3])'''